[package]
name = "gitsync"
version = "1.0.6"
edition = "2021"

[dependencies]
git2 = { version = "0.20", features = ["vendored-openssl", "vendored-libgit2"] }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
tempfile = "3.8"
